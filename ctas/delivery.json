{
  "schema": "ctas.delivery@1.0.0",
  "candidate_chunk_encoding": "identity",
  "note": "main and local previews serve plain JSON chunks; the GitHub Pages deployment rewrites this to gzip (scripts/compress_ctas_chunks.sh)"
}
